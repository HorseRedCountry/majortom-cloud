server:
  port: 2001

spring:
  application:
    name: seata-order-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://82.157.65.202:3306/seata_order?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
  cloud:
    nacos:
      discovery:
        server-addr: http://82.157.65.202:8848/
        namespace: a6fcef0a-9dd2-4f9a-a109-f63a8ec54e80
  config:
    import:
      - optional:nacos:seata-client-demo.yml
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.majortom.cloud.domain
logging:
  level:
    io:
      seata: info
feign:
  client:
    config:
      default:
        read-timeout: 5000
        connect-timeout: 5000
